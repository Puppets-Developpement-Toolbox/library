#Root
global:
  html_start:
  header:
  footer:
  html_end:
  svg:

menus:
  main:

templates:
  home:
    - !load sections/hero_banner
    - !load sections/slider:horizontal
    - !load sections/text_image
    - !load sections/text_image:square
    - !load sections/push:pictos
    - !load sections/quote
    - !load sections/push:cards
    - !load sections/super_section
    - !load sections/slider:quotes 